---
import Layout from "../components/Layout.astro";
import Dashboard from "../components/Dashboard.astro";
import { getDataFreshness, toIsoDate } from "../lib/data-freshness";

export const prerender = true;

const title = "AI Stats | Compare AI Model Price, Speed, and Benchmarks";
const description =
  "Compare AI models by price, speed, context, and benchmark results from Artificial Analysis, Epoch AI, SimpleBench, PoliBench, OpenRouter, and other named sources. No single winner score.";

const { updatedAt } = await getDataFreshness();
const dateModified = toIsoDate(updatedAt) ?? undefined;
---

<Layout title={title} description={description} dateModified={dateModified}>
  <Dashboard />
</Layout>
