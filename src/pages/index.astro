---
import Layout from "../components/Layout.astro";
import Dashboard from "../components/Dashboard.astro";
import { getDataFreshness, toIsoDate } from "../lib/data-freshness";

export const prerender = true;

const title = "AI Stats | Source-backed AI model selection by Jonathan R. Reed";
const description =
  "Choose an AI model by task and budget, then inspect source-backed pricing, speed, benchmark coverage, and freshness without an opaque universal score.";

const { updatedAt } = await getDataFreshness();
const dateModified = toIsoDate(updatedAt) ?? undefined;
---

<Layout title={title} description={description} dateModified={dateModified}>
  <Dashboard />
</Layout>
